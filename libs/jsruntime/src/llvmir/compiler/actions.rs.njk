// DO NOT EDIT THIS FILE BY HAND.
//
// This file was automagically generated with:
// template: {{ template }}

use super::Compiler;

pub enum Action {
    Undefined,
    Nop,
    Invoke(fn(&mut Compiler) -> Result<(), String>, &'static str),
}

{# Disable formatting in order to avoid ill-positioned single line comments. #}
#[rustfmt::skip]
pub static ACTIONS: [Action; {{ data | length }}] = [
    {%- for item in data %}
    // {{ item.rule }}
    {%- if item.action === 'undefined' %}
    Action::Undefined,
    {%- elif item.action === 'nop' %}
    Action::Nop,
    {%- else %}
    Action::Invoke(Compiler::{{ item.action }}, "{{ item.action }}"),
    {%- endif %}
    {%- endfor %}
];
