[package]
name = "bee-htmltokenizer"
version = "0.1.0"
edition = "2021"
authors = ["BEE project contributors"]
publish = false

[dependencies]
bytes = "1.4.0"
thiserror = "1.0.40"
tracing = "0.1.37"

[dev-dependencies]
anyhow = "1.0.70"
assert_matches = "1.5.0"
serde = { version = "1.0.158", features = ["derive"] }
serde_json = "1.0.94"

[lib]
bench = false
