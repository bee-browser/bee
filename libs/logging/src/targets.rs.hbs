// DO NOT EDIT THIS FILE BY HAND.
//
// This file was automagically generated by:
// {{@command}}

use super::Target;

{{#each targets}}
/// The target ID for `{{name}}`.
pub const {{symbol}}: Target = Target({{@index}});

{{/each}}
// Use `const fn len()` instead of `const LEN` in order to avoid conflicts between target symbols.
// The `len` package might be created in the future.
#[inline(always)]
pub const fn len() -> usize {
    {{length targets}}
}

#[inline(always)]
pub const fn name(id: usize) -> &'static str {
    const NAMES: [&'static str; {{length targets}}] = [
        {{#each targets}}
        "{{name}}",
        {{/each}}
    ];
    NAMES[id]
}

#[cfg(test)]
mod tests {
    use super::*;

    {{#each targets}}
    #[test]
    fn test_{{snakeCase symbol}}() {
        const NAME: &'static str = {{symbol}}.name();
        assert_eq!(NAME, "{{name}}");
    }

    {{/each}}
}
