#!/usr/bin/env node

'use strict';

const program = require('commander');

program
  .description(
    'A layout message source')
  .command(
    'html', 'Generate layout messages from a HTML document')
  .command(
    'text', 'Convert text dots into layout boxes')
  .parse(process.argv);
