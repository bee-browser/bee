description: |
  2 flex lines are created.

  The `cross-start` and `cross-end` directions are swapped.

css: |
  {{> flex_direction.css}}

  .flex {
    flex-wrap: wrap-reverse;
    align-content: flex-start;  /* for simplicity */
  }

  .flex > .blue {
    width: 300px;
  }

html: |
  <div class="flex yellow">
    <div class="red"></div>
    <div class="green"></div>
    <div class="blue"></div>
  </div>
