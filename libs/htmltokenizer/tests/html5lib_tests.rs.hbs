mod helper;

use helper::tokenize;
{{#each .}}

#[test]
fn test_{{padStart @index 4 '0'}}() {
    tokenize(
        #[allow(clippy::invisible_characters)]
        r##"{{json .}}"##
    );
}
{{/each}}
