[package]
name = "toydom"
version = "0.0.0"
edition = "2021"
authors = ["BEE project contributors"]
publish = false

[dependencies]
htmlparser = { path = "../htmlparser" }
logging = { path = "../logging" }

[lib]
bench = false
