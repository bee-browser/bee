[package]
name = "geometry"
version = "0.0.0"
authors = ["BEE project contributors"]
edition = "2021"
publish = false

[dependencies]
num-traits = "0.2.18"
serde = { version = "1.0.197", features = ["derive"], optional = true }

[dev-dependencies]
serde_test = "1.0.176"

[lib]
bench = false
