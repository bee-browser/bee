[package]
name = "estree"
version = "0.0.0"
edition = "2021"
authors = ["BEE project contributors"]
publish = false

[dependencies]
anyhow = "1.0.86"
assert-json-diff = "2.0.2"
clap = { version = "4.5.16", features = ["env", "derive", "wrap_help"] }
jsparser = { path = "../../libs/jsparser", features = ["location"] }
logging = { path = "../../libs/logging" }
serde = { version = "1.0.208", features = ["derive", "rc"] }
serde_json = { version = "1.0.126", features = ["preserve_order"] }
